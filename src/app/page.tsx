"use client"
import { NavigationMenu, Card, Button, Accordion, Form, Input, TabsList, Tabs, TabsTrigger, TabsContent, FormField, FormItem, FormLabel, NavigationMenuContent, NavigationMenuViewport, NavigationMenuItem, NavigationMenuLink, NavigationMenuList, NavigationMenuTrigger } from "@/components/ui/";
import { Mail, Phone, MapPin, Facebook, Twitter, Instagram, Monitor, Cloud, UserPlus } from "lucide-react";
import { useForm } from "react-hook-form";
import ComingSoon from "./comingsoon";

export default function HomePage() {
  return (<ComingSoon />);
}